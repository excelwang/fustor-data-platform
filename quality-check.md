# manual check these flaws to detect if the sensord has do a good job
  - [ ] may duplicate push
   compatibility
   For test
  - [ ] packages/core/src/sensord_core/common/logging_config.py 不能有下游包名
  - [ ]     print(f"DEBUG:
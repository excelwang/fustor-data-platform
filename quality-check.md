# manual check these flaws to detect if the datacast has do a good job
  - [ ] may duplicate push
   compatibility
   For test
  - [ ] packages/core/src/datacast_core/common/logging_config.py 不能有下游包名
  - [ ]     print(f"DEBUG:
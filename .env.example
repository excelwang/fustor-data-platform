# Fustor Environment Variables Example
# Copy this to ~/.fustor/.env or project root .env

# Base directory for all configurations (defaults to ~/.fustor)
# FUSTOR_HOME=/path/to/fustor/data

# fustord specific settings
FUSTOR_FUSION_SESSION_TIMEOUT_SECONDS=30

# Log Levels: DEBUG, INFO, WARNING, ERROR
FUSTOR_LOG_LEVEL=INFO